coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 1%
    patch:
      default:
        target: 80%

ignore:
  - "test/**/*"
  - "src/main.c"

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
